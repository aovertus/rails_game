<%= form_for(@game, :html =>{:multipart => true}) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |message| %>btn btn-default
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>

    <%= f.label :note %><br>
    <%= f.number_field :note %>
    
    <%= f.file_field :image %>
  	<%= f.hidden_field :user_id, :value => session[:user_id]%>
  </div>	
  <div class="actions">
    <%= f.submit "create ", :class => "btn btn-default" %>
  </div>
<% end %>
